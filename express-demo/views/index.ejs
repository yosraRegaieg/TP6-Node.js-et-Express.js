<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Accueil</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Bienvenue, <%= user %> !</h1>
    <nav>
      <a href="/">Accueil</a> |
      <a href="/tasks">Tâches (<%= tasks.length %>)</a>
    </nav>
    <p>Vous avez <strong><%= tasks.filter(t => !t.done).length %></strong> tâche(s) en cours.</p>
    <hr>
    <h2>Aperçu rapide</h2>
    <ul>
      <% tasks.slice(0, 3).forEach(task => { %>
        <li class="<%= task.done ? 'done' : '' %>">
          <%= task.title %> — <%= task.done ? 'Terminée' : 'En cours' %>
        </li>
      <% }) %>
    </ul>
  </div>
</body>
</html>